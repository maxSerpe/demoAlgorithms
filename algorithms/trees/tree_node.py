class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

    def binary_tree_to_list(head_node):
        """Convert a binary tree to a list. Unfortunately, the LeetCode format
        is somewhat non-standard, making it pointless to emmulate and test"""
        pass

    def list_to_binary_tree(list_input):
        """Convert a binary tree to a list. Unfortunately, the LeetCode format
        is somewhat non-standard, making it pointless to emmulate and test"""
        pass
